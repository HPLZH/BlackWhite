<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:p="clr-namespace:BlackWhite.App.Properties"
             x:Class="BlackWhite.App.MainPage">

    <StackLayout>
        <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
            <Label Text="{x:Static p:App.AppName}" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <ScrollView>
            <StackLayout Padding="20">
                <Button Text="{x:Static p:MainPage.Start}"></Button>
                <Button Text="{x:Static p:MainPage.Settings}"></Button>
                <Button Text="{x:Static p:MainPage.Statistics}"></Button>
                <Button Text="{x:Static p:MainPage.Help}"></Button>
                <Button Text="{x:Static p:MainPage.Version}"></Button>
                <Button Text="{x:Static p:MainPage.Language}"></Button>
            </StackLayout>
        </ScrollView>
    </StackLayout>

</ContentPage>
