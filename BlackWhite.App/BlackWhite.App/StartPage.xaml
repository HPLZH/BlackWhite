<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:p="clr-namespace:BlackWhite.App.Properties"
             xmlns:c="clr-namespace:BlackWhite.App"
             x:Class="BlackWhite.App.StartPage"
             Title="{x:Static p:StartPage.Title}">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="20">
                <c:ButtonCardView ButtonText="{x:Static p:StartPage.Next}" Title="{x:Static p:StartPage.Normal}" Text="{x:Static p:StartPage.Normal_Text}" ButtonClicked="Normal_ButtonClicked"></c:ButtonCardView>
                <c:ButtonCardView IsVisible="False" ButtonText="{x:Static p:StartPage.Next}" Title="{x:Static p:StartPage.Perfect}" Text="{x:Static p:StartPage.Perfect_Text}" ButtonClicked="Perfect_ButtonClicked"></c:ButtonCardView>
                <c:ButtonCardView IsVisible="False" ButtonText="{x:Static p:StartPage.Next}" Title="{x:Static p:StartPage.Timed}" Text="{x:Static p:StartPage.Timed_Text}" ButtonClicked="Timed_ButtonClicked"></c:ButtonCardView>
                <c:ButtonCardView IsVisible="False" ButtonText="{x:Static p:StartPage.Next}" Title="{x:Static p:StartPage.Free}" Text="{x:Static p:StartPage.Free_Text}" ButtonClicked="Free_ButtonClicked"></c:ButtonCardView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>